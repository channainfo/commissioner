<% content_for :page_title do %>
  <%= link_to Spree.t(:roles), spree.billing_roles_url %> /
  <%= Spree.t(:new_role) %>
<% end %>

<%= render partial: 'spree/admin/shared/error_messages', locals: { target: @role } %>

<%= form_for [:billing, @role] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/new_resource_links', locals: { collection_url: spree.billing_roles_path } %>
  </fieldset>
<% end %>