<div class="card-body">
  <div class="form-group mb-0">
    <%= label_tag Spree.t('customer_rule.choose_customers') %>
    <%= select "#{param_prefix}", 'customer_ids_string', options_from_collection_for_select(SpreeCmCommissioner::Customer.all, :id, :fullname, promotion_rule.customers.pluck(:id)), { include_hidden: true }, multiple: true, class: 'select2 d-block w-100' %>
  </div>
</div>